{% extends '@Main/Default/index.html.twig' %}
{% block content %}




    <link href="//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.2/css/bootstrap-combined.min.css" rel="stylesheet" id="bootstrap-css">
    <script src="//netdna.bootstrapcdn.com/twitter-bootstrap/2.3.2/js/bootstrap.min.js"></script>
    <script src="//code.jquery.com/jquery-1.11.1.min.js"></script>

    <link href="//netdna.bootstrapcdn.com/bootstrap/3.2.0/css/bootstrap.min.css" rel="stylesheet" id="bootstrap-css">
    <script src="//netdna.bootstrapcdn.com/bootstrap/3.2.0/js/bootstrap.min.js"></script>
    <script src="//code.jquery.com/jquery-1.11.1.min.js"></script>

    <div class="container" align="center">


        <div class="row">

            <div class="span5">
                <h2>Liste des offres </h2>
                <br>
                <button id="button1id" name="button1id" class="btn btn-success" onsubmit={{ path('offreadd') }} >Ajouter</button>
                <a href={{ path('offreadd') }} >ajouter</a>
                 <form method="post" >
        Objet Offre : <input type="text" name="objet">
        <br>


        <button type="submit">rechercher</button>


                <br>
                <table class="table table-striped table-condensed">
                    <thead>
                    <tr>
                        <th>Objet</th>
                        <th>Description</th>
                        <th>Prix (TND) </th>
                        <th>Date création</th>
                        <th>Etat</th>
                    </tr>
                    </thead>
                    <tbody>
                    <tr> {% for i in o %}
                        <td> {{ i.objet }}</td>
                        <td>{{ i.description}}</td>
                        <td>{{ i.prix}}</td>
                        <td>{{ i.datecreation}}</td>
                        {%  if (i.�tat==0) %}
                        <td><span class="label">En attente</span></td>
                        {% endif %}

                        {%  if (i.�tat==1) %}
                        <td><span class="label label-success">En cours</span>

                            {% endif %}

                            {%  if (i.�tat==2) %}
                        <td><span class="label label-success">Terminée</span>

                            {% endif %}
                        <td>
                            <a href={{ path('offremodif',{'id' : i.idOffre }) }}><i class="icon-pencil"></i></a>
                            <a role="button" data-toggle="modal" href={{ path('offresupp',{'id' : i.idOffre }) }} ><i class="icon-remove"></i></a>
                        </td>


                    </tr>
                    {% endfor %}

                    </tbody>
                </table>
            </div>
        </div>
    </div>



{% endblock %}